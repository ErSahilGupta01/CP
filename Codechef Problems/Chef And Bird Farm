#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{  
   ll t;
   cin>>t;
   
   while(t--)
   { ll a,b,c;
   cin>>a>>b>>c;
     if((c%a==0)||(c%b==0))
     {
         if((c%a!=0)&&(c%b==0))
         {
             cout<<"DUCK"<<endl;
         }
         else if((c%a==0)&&(c%b!=0))
         {
             cout<<"CHICKEN"<<endl;
         }
         else
         cout<<"ANY"<<endl;
     }
     else cout<<"NONE"<<endl;
     
     
     
   }

    return 0;
}
