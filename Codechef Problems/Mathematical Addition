#include <bits/stdc++.h>
using namespace std;
int main()
{   ios::sync_with_stdio(0);
    cin.tie(0);
    int T;
    cin>>T;
    while(T--)
    {
       long long  int u,v;
         cin>>u>>v;
         cout<<(-(u*u))<<"\t"<<v*v<<endl;
         
         
    }
 
    return 0;
}
