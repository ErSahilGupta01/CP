int main() {
   
int a[10],max,s;

for (int i=0;i<6;i++)
  { 
    scanf("%d",&a[i]);
  }

 max=a[0];
    s=a[0];
    for  (int i=0;i<6;i++)
    {
        if(a[i]>max)
{ s=max;
            max=a[i];
            
            
}
        if(a[i]<max&&a[i]>s)
        {
            s=a[i];
        }
    }
printf("largest=%d",max);
    printf(" Second largest=%d",s);
    return 0;
}
