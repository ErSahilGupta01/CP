#include <stdio.h>

#include<math.h>

int main()

{ int num,rev,i=0,b;
  
  scanf("%d",&num);
  
 while(num>0)

{
    rev =rev+num%2*pow(10,i++);

num=num/2;

    
}

printf("%d",rev);
 
   return 0;
}
